
let a = 1.2;
println(a);

let err = 1e^15;
println(err);

let err = 1e-15;
println(err);

let a = 2;
let err = 1e-15;
let root = a;

while math.abs(a - root*root) > err {
  root = (a/root + root) / 2.0;
}
println("square root of 2 is: " + root)